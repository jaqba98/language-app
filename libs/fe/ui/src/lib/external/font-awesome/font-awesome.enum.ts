export enum FontAwesomeTypeEnum {
  school = 'school',
  lock = 'lock',
  play = 'play',
  star = 'star',
  bars = 'bard',
}

export enum FontAwesomeColorEnum {
  default = 'default',
  gray = 'gray',
  green = 'green',
  gold = 'gold',
}
