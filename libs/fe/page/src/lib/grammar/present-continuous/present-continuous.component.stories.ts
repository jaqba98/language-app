import type { Meta, StoryObj } from '@storybook/angular';

import { PresentContinuousComponent } from './present-continuous.component';

const meta: Meta<PresentContinuousComponent> = {
  component: PresentContinuousComponent,
  title: 'PresentContinuousComponent',
};
export default meta;
type Story = StoryObj<PresentContinuousComponent>;

export const Primary: Story = {};
